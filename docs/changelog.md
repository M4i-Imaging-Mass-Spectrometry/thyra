!!! info "Generated, not written"
    This page is the repository's `CHANGELOG.md`, included verbatim.
    python-semantic-release rewrites that file from the commit messages at
    every release, so it is never edited by hand -- and because this page
    includes it rather than copying it, the two cannot drift apart. Thyra
    follows [Semantic Versioning](https://semver.org/) and
    [Conventional Commits](https://www.conventionalcommits.org/).

--8<-- "CHANGELOG.md"
